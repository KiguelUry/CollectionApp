Fichiers audio du splash :
- splash_guitar_soft.wav — arpège doux type guitare/cordes, ~1,8 s — priorité actuelle
- splash_welcome.wav — arpège doux (Do-Mi-Sol-Do aigu), ~1 s — priorité actuelle
- splash_chime.wav — jingle généré (accord Do-Mi-Sol, ~0,5 s)
- splash_chime.mp3 — optionnel

Sans fichier, l'app retombe sur un bip système.

Ancien splash visuel (violet / orbite) : voir git history ou `collectingo_splash.dart` avant refonte juin 2026.
