# Copier ce fichier vers .env à la racine du projet, puis remplir les valeurs.
# Ne jamais committer le fichier .env (il est dans .gitignore).

SUPABASE_URL=
SUPABASE_ANON_KEY=
BGG_APPLICATION_TOKEN=
